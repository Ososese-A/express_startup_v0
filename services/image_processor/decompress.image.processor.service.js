const { logToConsole } = require('../utility/sample.utility')

// same goes with decomprerssion, one with zlib and one without

module.exports = {
    compressOneStep: async () => {
    },

    compressTwoStep: async () => {
    }
}